// Fill this in with your own Firebase project's web config to enable cloud
// sync (Project settings > General > Your apps > SDK setup and
// configuration, in the Firebase console). These values are safe to publish
// in client-side code — access to your data is enforced by Firestore
// security rules (see firestore.rules), not by keeping this config secret.
//
// Leave the placeholder values as-is to keep the app running in
// localStorage-only mode (no cloud sync, nothing changes).
const firebaseConfig = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT_ID.firebaseapp.com',
  projectId: 'YOUR_PROJECT_ID',
  storageBucket: 'YOUR_PROJECT_ID.appspot.com',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID',
};
